<% if model.errors.any? %>
   <div class="alert alert-warning">
       <ul>
           <% model.errors.full_messages.each do |message| %>
             <il><%= message %></il>
           <% end %>
       </ul>
    </div>
<% end %>